# 02. Доменная модель

## 2.1 Главная идея: один реестр, много типов

Монитор, кластер БД и SaaS-лицензия учитываются по-разному, но ими одинаково владеют,
их одинаково выдают, у них одинаково истекают сроки. Поэтому модель строится как
**единая сущность `Resource` + тип ресурса со схемой атрибутов**, а не как десяток
несвязанных таблиц.

```
ResourceType (справочник, описывает схему)
      │ 1..N
      ▼
  Resource  ──── attributes (значения по схеме типа)
      │
      ├── ownership: владелец, пользователи, команда
      ├── lifecycle: статус, даты, стоимость
      └── relations: связи с другими ресурсами (граф)
```

## 2.2 Основные сущности

### Resource
Учётная единица. Общие поля для всех типов:

| Поле | Описание |
|---|---|
| `id` | Идентификатор |
| `type` | Ссылка на `ResourceType` |
| `name` | Человекочитаемое имя |
| `code` | Инвентарный/внутренний номер, уникален |
| `status` | Статус жизненного цикла (см. 2.4) |
| `criticality` | `low` / `medium` / `high` / `critical` |
| `environment` | `prod` / `stage` / `dev` / `office` |
| `owner_id` | Ответственный (сотрудник) |
| `org_unit_id` | Команда/отдел, на балансе которого ресурс |
| `location_id` | Локация (для физических) |
| `provider_id` | Провайдер/поставщик (для облачных и закупок) |
| `external_id` | ID в системе-источнике (ARN, resource id, серийник) |
| `cost_amount`, `cost_currency`, `cost_period` | Стоимость: разовая или периодическая |
| `acquired_at`, `expires_at`, `decommissioned_at` | Ключевые даты |
| `tags` | Свободные метки |
| `attributes` | Значения атрибутов по схеме типа (JSON) |
| `allowed_maintenance_window` | Необязательное правило допустимого времени обслуживания: часовой пояс и недельные интервалы; см. раздел 07, §7.6 |
| `created_at`, `updated_at` | Служебные |

### ResourceType
Описывает категорию и схему её атрибутов: `key`, `name`, `category`
(`hardware` / `cloud` / `software` / `service` / `data` / `network`), `icon`,
`attribute_schema` (список полей: ключ, тип, обязательность, справочник значений),
`is_assignable` (можно ли выдать на руки сотруднику).

Примеры типов и специфичных атрибутов:

| Категория | Типы | Специфичные атрибуты |
|---|---|---|
| hardware | ноутбук, монитор, док-станция, телефон, сервер, сетевое оборудование | серийный номер, модель, диагональ, разрешение, MAC, гарантия до, поставщик |
| cloud | виртуальная машина, кластер k8s, объектное хранилище, балансировщик | регион, инстанс-тип, vCPU/RAM, публичный IP, аккаунт провайдера |
| data | база данных, очередь, кэш, бэкап-политика | движок и версия, размер, RPO/RTO, шифрование, окно бэкапа |
| service | внутренний сервис, SaaS, API | URL, репозиторий, runbook, SLA, дежурная команда |
| software | лицензия, подписка | вендор, тип лицензии, число мест, срок действия |
| network | домен, TLS-сертификат, VPN, IP-подсеть | регистратор, CN/SAN, издатель, дата истечения |

### User
Сотрудник: `id`, ФИО, email, `org_unit_id`, должность, `manager_id`, статус
(`active` / `on_leave` / `offboarded`), роли, внешние идентификаторы (SSO, логин в трекере,
ID в мессенджере для оповещений).

### OrgUnit
Организационная единица: отдел/команда/проект. Иерархия через `parent_id`.
Используется и для видимости данных, и для отнесения затрат.

### Assignment (владение/пользование)
Факт закрепления ресурса за пользователем: `resource_id`, `user_id`, `role`
(`owner` / `user` / `backup_owner`), `assigned_at`, `returned_at`, `assigned_by`,
`acceptance_status` (сотрудник подтвердил получение или нет). История не удаляется —
по ней видно, у кого ресурс был раньше.

### ResourceRelation (ребро графа)
`source_id`, `target_id`, `relation_type`, `attributes`. Типы связей:

| Тип | Смысл | Пример |
|---|---|---|
| `depends_on` | источник не работает без цели | сервис → БД |
| `hosted_on` | размещён на | БД → виртуальная машина |
| `connected_to` | подключён к | монитор → док-станция |
| `part_of` | входит в состав | нода → кластер |
| `licensed_by` | покрыт лицензией | рабочее место → лицензия |
| `backup_of` | является резервом | реплика → мастер |
| `secured_by` | защищён / обслуживается | домен → TLS-сертификат |

Связи направленные; граф в общем случае — ориентированный, циклы возможны и должны
корректно обрабатываться при обходе.

### Request (заявка)
Обращение сотрудника: `type` (`grant` / `return` / `change` / `decommission` / `new_purchase`),
`requester_id`, `resource_id` или `resource_type_id` (если ресурса ещё нет),
`justification`, `status` (`draft` → `pending_approval` → `approved` / `rejected` →
`in_progress` → `done` / `cancelled`), `approver_id`, ссылка на задачу в трекере.

### Регламентные работы

`MaintenanceWindow` — отдельное окно работ: плановые и фактические сроки,
исполнитель, срочность, состояние выполнения и результат. `MaintenanceResource`
связывает окно с ресурсом и хранит его результат. Поле `prev_status` содержит состояние
перед текущим окном, `restore_status` — подтверждённое состояние до непрерывного
пребывания в обслуживании. Поле `previous_maintenance_id` связывает последовательные
окна по одному ресурсу; основание возврата сохраняется по правилам раздела 07, §7.3.
`MaintenanceTemplate` задаёт расписание и условия повторяемых работ; созданные окна
сохраняют копию условий. `occurrence_at` хранит неизменный момент по расписанию;
пара `(template_id, occurrence_at)` уникальна даже при переносе плановых сроков.

Результат выполнения и решение согласующего хранятся отдельно от состояния окна.
Согласование связано с редакцией плана; неудачный результат работ не отождествляется
с отказом в согласовании. С внешней задачей окно связано через `TrackerLink`.
Подробные поля, переходы и ограничения приведены в [разделе 07](07-maintenance.md).

### AuditLog
Неизменяемый журнал: кто, когда, какую сущность и какие поля изменил (before/after),
источник изменения (UI / API / импорт / интеграция).

### Вспомогательные справочники
`Location` (площадка, кабинет, стойка), `Provider` (AWS/Yandex Cloud/поставщик железа),
`Vendor`, `CostCenter`.

## 2.3 Схема связей (обзор)

```
OrgUnit ──< User ──< Assignment >── Resource >── ResourceType
   │                                  │  │
   │                                  │  └──< ResourceRelation >── Resource
   └──────────< Resource              │
                                      └──< Request ──> (внешняя задача в трекере)
                                      └──< AuditLog
                                      └──< MaintenanceResource >── MaintenanceWindow
                                                                       │
                                                                       └──> MaintenanceTemplate
```

## 2.4 Жизненный цикл ресурса

- `planned` — заявлен, но не куплен/не создан.
- `ordered` — закуплен/заказан, ещё не поступил.
- `in_stock` — на складе, свободен.
- `assigned` — закреплён, но сотрудник ещё не подтвердил получение.
- `in_use` — активно используется.
- `maintenance` — ресурс находится на обслуживании. Это учётное состояние,
  а не результат проверки доступности; остановка облачного ресурса сама по себе
  не подтверждает начало регламентных работ.
- `returned` — сдан сотрудником, ожидает приёмки IT.
- `decommissioned` — списан/удалён (терминальный).
- `lost` — утерян.

### Разрешённые переходы

| Из | В | Кто | Комментарий |
|---|---|---|---|
| `planned` | `ordered`, `decommissioned` | IT | Заказан или отменён |
| `ordered` | `in_stock`, `decommissioned` | IT | Поступил или отказ от поставки |
| `in_stock` | `assigned`, `maintenance`, `decommissioned`, `lost` | IT | Выдача со склада |
| `assigned` | `in_use`, `returned`, `in_stock`, `lost` | Сотрудник / IT | Подтверждение получения, либо отказ до подтверждения |
| `in_use` | `returned`, `maintenance`, `assigned`, `lost`, `decommissioned` | Сотрудник / IT | Сдача, ремонт, передача другому, прямое списание |
| `maintenance` | `in_use`, `in_stock`, `returned`, `decommissioned`, `lost` | ИТ-отдел | Возврат в `returned` допустим при завершении окна, только если `restore_status` имеет значение `returned` по правилам раздела 07, §7.3; остальные переходы проверяются по общим правилам |
| `returned` | `in_stock`, `maintenance`, `decommissioned`, `lost` | IT | Приёмка: годен, в ремонт или списать |
| `lost` | `in_stock`, `decommissioned` | IT | Нашёлся или окончательно списан |
| `decommissioned` | — | — | Терминальный |

### Почему из `in_use` нет прямого пути в `in_stock`

Между ними намеренно стоит `returned`: возврат двухшаговый. Сотрудник сдаёт ресурс —
IT принимает и оценивает состояние. Если разрешить прямой переход, склад будет
считать свободным то, что физически лежит у сотрудника или уехало в ремонт, и на это
можно выписать вторую выдачу. `returned` — это «сдан, но ещё не проверен».

Исключения, где приёмка не нужна:
- `in_use → maintenance → in_stock` — ресурс уехал в ремонт прямо от сотрудника.
- `in_use → decommissioned` — списание без возврата на склад (сломан, устарел).
- Для неназначаемых типов (`is_assignable = false`: облачные ВМ, БД, сервисы)
  состояния `assigned` / `returned` не используются вовсе — у них рабочий цикл
  `in_stock → in_use → maintenance → decommissioned`.

Каждый переход пишется в аудит. Переходы с колонкой «IT» доступны только ролям
`it_specialist` / `it_admin`; сотрудник может инициировать лишь `assigned → in_use`,
`in_use → returned` и пометить ресурс утерянным.

### Состояние ресурса при регламентных работах

Начало окна допускается для ресурсов в состояниях `in_stock`, `in_use`, `returned`
и `maintenance`; ограничения по типу ресурса сохраняются. Состояние `maintenance`
допускает новое окно только при отсутствии другого выполняемого окна над ресурсом.
Запланированное окно не меняет состояние ресурса.

При начале сохраняется состояние каждого ресурса перед текущим окном (`prev_status`);
основание возврата (`restore_status`) определяется по разделу 07, §7.3. Зависимые ресурсы
и соседи по серверу автоматически в обслуживание не переводятся. Пока окно
выполняется, другие операции не меняют состояния его ресурсов; импорт не является
исключением. Завершение требует подтверждённого результата и допустимого итогового
состояния по каждому ресурсу. Автоматический безусловный возврат к прежнему
состоянию запрещён; частичный или неудачный результат оставляет ресурс в обслуживании.

## 2.5 Правила целостности

- У ресурса в статусах `assigned` / `in_use` обязан быть активный `Assignment` с ролью `owner`.
- Нельзя удалить ресурс, на который есть входящие связи, — сначала списание или перенос связей.
- `code` уникален в пределах организации; `external_id` уникален в пределах провайдера.
- Ресурс участвует непосредственно не более чем в одном выполняемом окне работ.
  Проверка этого ограничения и запись начала работ выполняются в одной транзакции.
- Ресурс с `expires_at` в прошлом и статусом `in_use` подсвечивается как нарушение.
- Ресурс категории `service` / `data` должен иметь связь `hosted_on` (или быть managed-услугой
  провайдера) — иначе неизвестно, где он запущен, и он попадает в отчёт «без хоста».
- Офбординг сотрудника блокируется, пока за ним числятся ресурсы в активных статусах.

## 2.6 Расширяемость

Новый тип ресурса добавляется как запись `ResourceType` с описанием атрибутов —
без изменения кода и схемы БД. Это ключевое требование: «мониторы, сервисы, БД и др.»
должны заводиться администратором, а не разработчиком.
